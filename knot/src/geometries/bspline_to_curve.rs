// See implementation here: https://en.wikipedia.org/wiki/De_Boor%27s_algorithm#Example_implementation 

fn get_point(
    t: f64, // Time t between 0 and 1
    knot_positions: &[f64], // Array of knot positions
    control_points: &[f64], // Array of control points
    degree: i32
) {
    // Pad knot positions
    // Determine which interval of knot positions t is in

    // Create array of temporary control points
}
